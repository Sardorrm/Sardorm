# MindShift runtime

Godot 4 Android-first runtime.

`main.gd` is the current scene controller. `main_full.gd` contains the complete playable puzzle-loop implementation staged for integration. `puzzle_engine.gd`, `save_manager.gd`, and `stats_manager.gd` provide the core services.
